from pydantic import BaseModel

# TODO: Create Booking model
# Fields:
# - user_id: int
# - room_id: int
# - nights: int (must be greater than 0)
# - rate_per_night: float (must be greater than 0)
# - Also, add computed fields: total_amount = nights * rate_per_night
